{
  "name": "threadline-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:web": "npm run dev --workspace=apps/ui",
    "scraper:dev": "npm run dev --workspace=apps/scraper",
    "tauri": "tauri",
    "build": "node scripts/build.js",
    "build:win": "node scripts/build.js",
    "icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "@vitejs/plugin-react": "^5.1.1",
    "concurrently": "^8.0.0",
    "vite": "^7.2.4"
  }
}
