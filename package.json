{
    "name": "threadline-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "src-tauri"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=apps/ui",
        "scraper:dev": "npm run dev --workspace=apps/scraper",
        "tauri": "tauri"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^1.5.0",
        "concurrently": "^8.0.0"
    }
}